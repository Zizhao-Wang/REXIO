



/**
 *  global variables for all processes (threads). 
 **/

extern struct nvm_bp* bp;

extern const struct nvm_geo *geo;

/**
 * global variables for backend processes (threads). 
 */


/**
 * global variables for frontend processes (threads). 
 */